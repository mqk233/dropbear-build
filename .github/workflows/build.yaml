name: Build with debian
on:
  workflow_dispatch:
    inputs:
      release_tag:
        type: string
        required: true
jobs:
  build:
    runs-on: ${{ matrix.os }}
    container:
      image: debian:13
      env:
        LANGUAGE: en_US:en
        LANG: en_US.UTF-8
    strategy:
      matrix:
        include:
          - os: "ubuntu-24.04"
          - os: "ubuntu-24.04-arm"
    env:
      OPENSSL_VERSION: "3.5.5"
      OPENSSL_PREFIX: "/usr/local/openssl"
    steps:
      - name: Build openssl
        run: locale
