name: Build
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout the code
        uses: actions/checkout@v6
        with:
          repository: "pymumu/smartdns"
      - name: Build package
        run: |
          /home/runner/work/dropbear-build/dropbear-build/package/build-pkg.sh --platform linux --static --only-package --outputdir /home/runner/work/dropbear-build/dropbear-build
          ls -al
